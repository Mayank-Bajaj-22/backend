express ka use karke server banayenge

    <!-- Initializing node prjoect -->
        - Step 1 : npm init -y
        - Step 2 : make npm command to run file in package.json
            - "start": "node index.js"

    - before deploying install npm i dotenv
    - .env